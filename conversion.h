#ifndef NUMBERS_CONVERSION_H
#define NUMBERS_CONVERSION_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>
#include <ctype.h>

#define M 100

int CharToInt(char ch);

char IntToChar(int ch);

double func1(int b1, char *st);

int func2(int b2, double D, char *end);

#endif //NUMBERS_CONVERSION_H
